/var/log/haproxy.log {
    daily
    rotate 10
    missingok
    notifempty
    compress
    delaycompress
    postrotate
        /bin/kill -15 $(pidof rsyslogd) &>/dev/null || sleep 3 && rm -f /var/run/rsyslogd.pid && rsyslogd
    endscript
}
